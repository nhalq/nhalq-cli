#/usr/bin/env bash

autobot_completion() {
  if [ "${#COMP_WORDS[@]}" == "1" ]; then
    COMPREPLY=("vpn")
    return
  fi

  COMPREPLY=("connect" "disconnect")
}

complete -F autobot_completion autobot
